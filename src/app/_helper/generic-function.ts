export class SessionStorage {
    static setSessionStorage(key: string, Value: string) {
        SessionStorage.setSessionStorage(key, Value);
    }
    
    static getSessionStorage(key: any): any {
        return SessionStorage.getSessionStorage(key);

    }
}
